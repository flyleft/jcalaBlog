package me.jcala.blog.service;

import me.jcala.blog.service.inter.AdminBlogSerInter;

/**
 * Created by Administrator on 2016/9/3.
 */
public class AdminBlogSer implements AdminBlogSerInter {
    @Override
    public void addBlog(String title, String tags, String article) {
/*
SimpleDateFormat simpleTime = new SimpleDateFormat("yyyy-MM-dd HH:mm:ss");
String passTime = pwoEditForm.getPasstime();
java.util.Date passUtilDate = simpleTime.parse(passTime);
java.sql.TimeStamp passSqlDate = new java.sql.TimeStamp(passUtilDate.getTime());
 */
    }
}
